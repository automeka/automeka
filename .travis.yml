language: cpp
script:
  - git submodule update --init --recursive
  - ninja
  - ./build/bin/meka/automeka
compiler:
  - clang
before_install:
  - sudo add-apt-repository -y "deb http://archive.ubuntu.com/ubuntu/ trusty main universe"
  - sudo add-apt-repository -y "deb http://llvm.org/apt/trusty/ llvm-toolchain-trusty main"
  - wget -O - http://llvm.org/apt/llvm-snapshot.gpg.key | sudo apt-key add -
  - sudo apt-get -qq update; sudo apt-get -qq install aptitude; sudo aptitude -yq2 update
  - sudo aptitude -yq2 full-upgrade -t trusty python
  - sudo aptitude -yq2 install -t trusty clang-3.6 ninja-build libboost1.54-all-dev
